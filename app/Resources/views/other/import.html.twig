{% extends 'base.html.twig' %}



{% block body %}

    {{ form_start(form) }}
    {% if error %}
        <div class="alert alert-danger">
            {{ error }}
        </div>
    {% endif %}
    {{ form_widget(form) }}

    <input type="submit" />
    {{ form_end(form) }}    

{% endblock %}