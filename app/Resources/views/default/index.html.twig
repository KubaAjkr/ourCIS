{% extends 'base.html.twig' %}



{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-md-10 col-md-offset-1">
                <div class="panel panel-success">
                    <div class="card-header">Hlavní stránka aplikace ourCIS</div>
                    {% if app.user == null %}
                        <div class="alert alert-info" role="alert">
                            k této aplkaci je nutné se přihlásit
                        </div>
                    {% else %}
                        <div class="alert alert-info" role="alert">
                            informace pro přihlášené uživatele
                        </div>
                    {% endif %}
                </div>


            </div>
        </div>
    </div>
{% endblock %}